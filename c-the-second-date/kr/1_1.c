/*
 * in the book example, there is no function type, which will produce a warning <warning: return type defaults to ‘int’>
 * return 0; is optional, since the default return value is 0?
 *
 * */

#include <stdio.h>

int main(){
    printf("Hello, World!\n");
    printf("\\c");
    return 0;
}